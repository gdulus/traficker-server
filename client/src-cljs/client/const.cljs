(ns client.const)

(def maps {:country "Germany"
           :overlay_id "animation_overlay"
           :map_id "map_canvas"})

(def push {:bus_name "browser.tcp"
           :server_address "http://localhost:8081/eventbus"})